# AccountLockouts
Display recent AD user lockouts on a webpage with php and powershell
This is a simple setup to display recent account lockouts through a web page.
You will need MySQL DB to store the events and the PHP will pull information from it.
